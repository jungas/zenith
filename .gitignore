node_modules/
.DS_Store
*.log
.cache/

# Build output: `npm run build` regenerates both.
dist/
dist-artifact/
.artifact-build/
/sw.js

# Server data: the SQLite budget store (see server/db.ts). Never commit this —
# it's the live database, not a fixture.
/data/
